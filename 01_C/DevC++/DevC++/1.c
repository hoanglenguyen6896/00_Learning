#include "stdio.h"
#include "conio.h"
#include "math.h"
/*void snt(int a)
{
	int i; int m=0;
	if (a==1 || a==0) printf("Khong phai so nguyen to");
	else if(a==2) printf("La so nguyen so");
	else
	{
		for(i=2; i<=sqrt(a); i++)
		{
			if (a%i==0) m++;
		}
		if (m==0) printf("La so nguyen to");
		else printf("Khong la so nguyen to");
	}
}*/
main()
{
	int a;
	printf("Nhap a="); scanf("%d", a);
}
